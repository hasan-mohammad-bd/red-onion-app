import React from 'react';
import { Navigate as Link, Outlet } from 'react-router-dom';
import "./Products.css"

const Products = () => {
    return (
        <div>
            

        </div>
    );
};

export default Products;